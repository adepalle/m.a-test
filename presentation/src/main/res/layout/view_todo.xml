<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/user_item_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/user_item_padding">

    <TextView
        android:id="@+id/todo_title_label"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="4"
        android:layout_marginEnd="@dimen/base_margin"
        tools:text="Lorem ipsum "
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/todo_status_label"
        app:layout_constraintTop_toTopOf="@+id/todo_status_icon"
        app:layout_constraintBottom_toBottomOf="@+id/todo_status_icon"/>

    <TextView
        android:id="@+id/todo_status_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/base_margin_double"
        android:layout_marginEnd="@dimen/base_margin"
        android:textStyle="bold"
        app:layout_constraintEnd_toStartOf="@+id/todo_status_icon"
        app:layout_constraintTop_toTopOf="@+id/todo_status_icon"
        app:layout_constraintBottom_toBottomOf="@+id/todo_status_icon"
        android:textColor="@color/red"
        tools:text="À Faire" />

    <ImageView
        android:id="@+id/todo_status_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/base_margin_half"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/red"
        tools:src="@drawable/todo_pending" />

</androidx.constraintlayout.widget.ConstraintLayout>